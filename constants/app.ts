// APP_NAME is a static constant — safe to use in both server and client components
export const APP_NAME = 'Admin B2B'

// NEXT_PUBLIC_* vars are inlined by Next.js at build time — safe for client components
export const APP_VERSION = process.env.NEXT_PUBLIC_APP_VERSION ?? '0.0.0'
export const APP_AUTHOR = process.env.NEXT_PUBLIC_APP_AUTHOR ?? ''
